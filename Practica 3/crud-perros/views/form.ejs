<!DOCTYPE html>
<html lang="es">
<head>
    <title><%= perro.id ? 'Editar' : 'Nuevo' %> Libro</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="container mt-4">
    <h1><%= perro.id ? 'Editar' : 'Nuevo' %> Libro</h1>
    <form action="<%= perro.id ? '/perros/editar/' + perro.id : '/perros/nuevo' %>" method="POST">
        <% if (perro.id) { %>  
            <input type="hidden" name="_method" value="PUT">
        <% } %>
        <div class="mb-3">
            <label class="form-label">Titulo:</label>
            <input type="text" name="nombre" class="form-control" value="<%= perro.nombre || '' %>" required>
        </div>
        <div class="mb-3">
            <label class="form-label">Autor:</label>
            <input type="text" name="raza" class="form-control" value="<%= perro.raza || '' %>" required>
        </div>
        <div class="mb-3">
            <label class="form-label">Anio de publicacion:</label>
            <input type="number" name="edad" class="form-control" value="<%= perro.edad || '' %>" required>
        </div>
        <button type="submit" class="btn btn-success">Guardar</button>
        <a href="/perros" class="btn btn-secondary">Cancelar</a>
    </form>
</body>
</html>

